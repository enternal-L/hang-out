import React from 'react'

const cardMock = (color) => {
  return (
        <div className='w-full h-full rounded-md border-black border-[3px] cursor-pointer' style={{background : color}}>
            
        </div>
  )
}

export default cardMock